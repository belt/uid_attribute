name: Feature Request
description: Suggest a new feature or improvement
labels: ["enhancement"]
body:
  - type: markdown
    attributes:
      value: |
        Describe the feature you'd like and why it would be useful.

  - type: textarea
    id: problem
    attributes:
      label: Problem or Use Case
      description: What problem does this solve? What are you trying to do?
    validations:
      required: true

  - type: textarea
    id: solution
    attributes:
      label: Proposed Solution
      description: How do you think this should work? Include API examples if possible.
      placeholder: |
        ```ruby
        # Example of the desired API
        class Order < ActiveRecord::Base
          include UidAttribute::ActiveRecordIntegration
          uid_attribute :public_id, version: 7, prefix: "ord_"
        end
        ```
    validations:
      required: true

  - type: textarea
    id: alternatives
    attributes:
      label: Alternatives Considered
      description: Other approaches you've thought about or workarounds you're using.

  - type: dropdown
    id: area
    attributes:
      label: Area
      description: Which part of the project does this affect?
      options:
        - Generator (UUID creation/validation)
        - ActiveRecord integration
        - PORO integration
        - ActiveModel Type
        - Refinements (CoreExt)
        - Documentation
        - Tooling / CI
        - Other

  - type: textarea
    id: context
    attributes:
      label: Additional Context
      description: Links, references, RFC sections, related issues.
